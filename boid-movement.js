function turnToAngle(targetAngle){
  
}

//turn by some increment (turn radius) per time to the target angle
  //at every increment calculate velocity from rotation vector.

//rate 2PI radians per second
//
